export function OverviewPage() {
  return (
    <section>
      <p className="eyebrow">PR-002</p>
      <h2>App shell + routing</h2>
      <p>
        Base shell and route structure are in place. Next milestone is command
        palette support.
      </p>
    </section>
  );
}
