export function App() {
  return (
    <main className="app">
      <header className="hero">
        <p className="eyebrow">Personal HQ</p>
        <h1>0 → 1 build initialized</h1>
        <p>
          Vite + React + TypeScript foundation is ready. Next step: App shell and
          routing.
        </p>
      </header>
    </main>
  );
}
